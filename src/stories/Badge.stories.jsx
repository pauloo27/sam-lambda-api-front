import { Badge } from "../components/Badge";
import '../index.css';

export default {
  title: "Components/Badge",
  component: Badge,
  tags: ["autodocs"],
};

export const Default = {
  args: {
    children: "Default",
  },
};
